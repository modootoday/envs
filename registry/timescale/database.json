{
  "name": "timescale/database",
  "version": 1,
  "title": "Timescale Cloud — service",
  "keys": {
    "TIMESCALE_DATABASE_URL": {
      "required": true,
      "sensitivity": "secret",
      "pattern": "^postgres(ql)?://\\S+$",
      "obtain": "https://console.cloud.timescale.com/",
      "description": "Postgres connection string with the password inside it."
    }
  }
}
